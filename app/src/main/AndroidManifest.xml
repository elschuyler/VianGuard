<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="org.eu.exodusprivacynetguard">

    <!-- === NETWORK PERMISSIONS === -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!-- === FOREGROUND SERVICE PERMISSIONS === -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <!-- === NETSPEED OVERLAY PERMISSION === -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- === BOOT AND SYSTEM PERMISSIONS === -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- === APPLICATION === -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        tools:targetApi="34">

        <!-- === MAIN ACTIVITY === -->
        <activity
            android:name=".ActivityMain"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- === SETTINGS ACTIVITY === -->
        <activity
            android:name=".ActivitySettings"
            android:exported="false"
            android:theme="@style/AppTheme" />

        <!-- === LOG ACTIVITY === -->
        <activity
            android:name=".ActivityLog"
            android:exported="false"
            android:theme="@style/AppTheme" />

        <!-- === SINKHOLE SERVICE (NetGuard Core) === -->
        <service
            android:name=".ServiceSinkhole"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- === CLEAR ACCESS SERVICE === -->
        <service
            android:name=".ServiceClearAccess"
            android:enabled="true"
            android:exported="false" />

        <!-- === NETSPEED OVERLAY SERVICE === -->
        <service
            android:name=".service.NetSpeedOverlayService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- === NETSPEED NOTIFICATION SERVICE === -->
        <service
            android:name=".service.NetSpeedNotificationService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- === BOOT RECEIVER === -->
        <receiver
            android:name=".ReceiverBoot"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- === NETWORK STATE RECEIVER === -->
        <receiver
            android:name=".ReceiverPackage"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
                <action android:name="android.intent.action.PACKAGE_ADDED" />
                <action android:name="android.intent.action.PACKAGE_REMOVED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
